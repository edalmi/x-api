package server

import (
	"errors"

	"github.com/edalmi/x-api/internal"
	"github.com/edalmi/x-api/internal/config"
)

func configureLogger(cfg *config.Logger) (internal.Logger, error) {
	return nil, errors.New("error")
}
